import setuptools

setuptools.setup(
    name="ML00",
    version="0.0.6",
    author="AJO",
    packages=setuptools.find_packages(),
    python_requires='>=3.7',
    install_requires=['numpy >= 1.18']
)